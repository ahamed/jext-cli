#!/usr/bin/env php
<?php
if (php_sapi_name() !== 'cli') {
	echo "PHP-CLI not found!";
    exit;
}

require __DIR__ . '/../vendor/autoload.php';

/** Run the application. */
(new Ahamed\Jext\Application)->run($argv);